---
phase: 02-frontend-display
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [FRONT-01, FRONT-02, FRONT-03, FRONT-04, FRONT-05, FRONT-06, FRONT-07]

must_haves:
  truths:
    - "Shortcode renders visible tile grid on a WordPress page"
    - "Grid layout responds to viewport width changes"
    - "Copy button provides visual feedback"
    - "Empty state renders when no projects exist"
  artifacts: []
  key_links: []
---

<objective>
Verify the frontend display works correctly in a running WordPress environment.

Purpose: Confirm the shortcode renders correctly, the grid is responsive, and copy-to-clipboard works before marking Phase 2 complete.
Output: Visual confirmation that all frontend requirements are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-display/02-01-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify frontend display on WordPress</name>
  <files>None (verification only)</files>
  <action>
    Present the verification checklist to the user. This is a human-verify checkpoint -- no code changes.

    What was built: Complete [community-master] shortcode implementation with responsive tile grid showing logo, name, description, GitHub link, and optional one-line installer with copy-to-clipboard.

    How to verify:
    1. Activate the plugin on your WordPress instance (if not already active)
    2. Create a test page and add the shortcode `[community-master]`
    3. Empty state test: With no published community projects, verify the page shows "Noch keine Community-Projekte vorhanden." centered text
    4. Create a test project with Title, Description, GitHub URL, One-Line Installer, and Featured Image
    5. Tile display: Verify logo, heading, description, GitHub link (opens in new tab), installer code box, and Copy button
    6. Copy button: Click Copy, verify it changes to "Copied!" with green styling, reverts after ~2 seconds, clipboard contains the command
    7. Responsive layout: Resize browser -- 3 columns wide, 2 columns tablet, 1 column mobile
    8. Theme integration: Verify tiles use theme fonts and colors

    Resume signal: Type "approved" or describe any issues found.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <done>User confirms all frontend display requirements are met: tiles render, grid is responsive, copy works, empty state shows, theme integrates naturally.</done>
</task>

</tasks>

<verification>
Human verification of all FRONT-* requirements in a live WordPress environment.
</verification>

<success_criteria>
- User confirms the shortcode renders correctly
- User confirms responsive grid works at all breakpoints
- User confirms copy-to-clipboard works with visual feedback
- User confirms theme integration looks natural
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-display/02-02-SUMMARY.md`
</output>
