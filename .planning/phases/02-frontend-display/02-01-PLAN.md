---
phase: 02-frontend-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-shortcode.php
  - templates/tile-grid.php
  - templates/single-tile.php
  - assets/css/frontend.css
  - assets/js/copy-installer.js
  - community-master.php
  - includes/class-community-master.php
autonomous: true
requirements: [FRONT-01, FRONT-02, FRONT-03, FRONT-04, FRONT-05, FRONT-06, FRONT-07, SEC-02, SEC-03]

must_haves:
  truths:
    - "Shortcode [community-master] on a page renders a grid of project tiles"
    - "Each tile shows logo, name, description, and GitHub link"
    - "Grid is 3 columns on desktop, 2 on tablet, 1 on mobile"
    - "One-line installer appears only when set, in a copyable code box"
    - "Copy button changes to 'Copied!' with green styling for 2 seconds"
    - "Empty state message shows when no projects exist"
    - "All output is escaped with esc_html, esc_url, esc_attr"
  artifacts:
    - path: "includes/class-shortcode.php"
      provides: "Shortcode registration, asset registration, render logic"
      contains: "add_shortcode"
    - path: "templates/tile-grid.php"
      provides: "Grid wrapper template"
      contains: "cm-grid"
    - path: "templates/single-tile.php"
      provides: "Individual card template"
      contains: "cm-tile"
    - path: "assets/css/frontend.css"
      provides: "Responsive grid and card styles"
      contains: "grid-template-columns"
    - path: "assets/js/copy-installer.js"
      provides: "Clipboard copy with visual feedback"
      contains: "navigator.clipboard"
  key_links:
    - from: "includes/class-shortcode.php"
      to: "templates/tile-grid.php"
      via: "include inside ob_start/ob_get_clean"
      pattern: "include.*tile-grid\\.php"
    - from: "templates/tile-grid.php"
      to: "templates/single-tile.php"
      via: "include inside foreach loop"
      pattern: "include.*single-tile\\.php"
    - from: "includes/class-shortcode.php"
      to: "assets/css/frontend.css"
      via: "wp_register_style + wp_enqueue_style"
      pattern: "community-master-frontend"
    - from: "includes/class-shortcode.php"
      to: "assets/js/copy-installer.js"
      via: "wp_register_script + wp_enqueue_script"
      pattern: "community-master-copy"
    - from: "includes/class-community-master.php"
      to: "includes/class-shortcode.php"
      via: "new CM_Shortcode() in constructor"
      pattern: "new CM_Shortcode"
    - from: "community-master.php"
      to: "includes/class-shortcode.php"
      via: "require_once"
      pattern: "require_once.*class-shortcode"
---

<objective>
Implement the complete [community-master] shortcode with responsive tile grid, copy-to-clipboard installer, and theme-integrated styling.

Purpose: Visitors can browse all community projects as tiles on any WordPress page using the shortcode.
Output: 5 new files (shortcode class, 2 templates, CSS, JS) + 2 modified files (entry point, orchestrator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-display/02-CONTEXT.md
@.planning/phases/02-frontend-display/02-RESEARCH.md
@.planning/phases/01-plugin-core-admin/01-01-SUMMARY.md
@.planning/phases/01-plugin-core-admin/01-02-SUMMARY.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From community-master.php:
```php
define('COMMUNITY_MASTER_VERSION', '1.0.0');
define('COMMUNITY_MASTER_FILE', __FILE__);
define('COMMUNITY_MASTER_DIR', plugin_dir_path(__FILE__));

require_once COMMUNITY_MASTER_DIR . 'includes/class-community-master.php';
require_once COMMUNITY_MASTER_DIR . 'includes/class-cpt-project.php';
require_once COMMUNITY_MASTER_DIR . 'includes/class-meta-boxes.php';
require_once COMMUNITY_MASTER_DIR . 'includes/class-admin-columns.php';
```

From includes/class-community-master.php:
```php
class Community_Master {
    private function __construct() {
        add_action('init', [CM_CPT_Project::class, 'register']);
        add_action('init', [CM_CPT_Project::class, 'register_meta_fields']);
        new CM_Meta_Boxes();
        new CM_Admin_Columns();
    }
}
```

Meta field keys (from Phase 1):
- `_community_master_description` (sanitize_textarea_field)
- `_community_master_github_url` (esc_url_raw)
- `_community_master_installer` (sanitize_text_field)

CPT slug: `community_project`
CPT is `public => false` -- shortcode is the only frontend.
Sort order: `menu_order` column in `wp_posts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shortcode class, templates, and wiring</name>
  <files>
    includes/class-shortcode.php
    templates/tile-grid.php
    templates/single-tile.php
    community-master.php
    includes/class-community-master.php
  </files>
  <read_first>
    community-master.php
    includes/class-community-master.php
    .planning/phases/02-frontend-display/02-RESEARCH.md
  </read_first>
  <action>
    Create `includes/class-shortcode.php` with class `CM_Shortcode`:

    1. Constructor: register shortcode `community-master` via `add_shortcode('community-master', [$this, 'render'])` and hook `register_assets` on `wp_enqueue_scripts`.

    2. `register_assets(): void` method:
       - `wp_register_style('community-master-frontend', plugins_url('assets/css/frontend.css', COMMUNITY_MASTER_FILE), [], COMMUNITY_MASTER_VERSION)`
       - `wp_register_script('community-master-copy', plugins_url('assets/js/copy-installer.js', COMMUNITY_MASTER_FILE), [], COMMUNITY_MASTER_VERSION, ['strategy' => 'defer', 'in_footer' => true])`

    3. `render(array $atts): string` method:
       - Enqueue `community-master-frontend` style and `community-master-copy` script (conditional loading -- only on pages using shortcode)
       - Query projects: `get_posts(['post_type' => 'community_project', 'posts_per_page' => -1, 'orderby' => 'menu_order', 'order' => 'ASC', 'post_status' => 'publish'])`
       - If `empty($projects)`: return `<div class="cm-empty-state">` with `esc_html__('Noch keine Community-Projekte vorhanden.', 'community-master')` (FRONT-07)
       - Otherwise: `ob_start()`, include `COMMUNITY_MASTER_DIR . 'templates/tile-grid.php'`, return `ob_get_clean()`

    4. Add ABSPATH guard at top: `defined('ABSPATH') || exit;`

    Create `templates/tile-grid.php`:
    - ABSPATH guard
    - `<div class="cm-grid">` wrapper
    - `foreach ($projects as $project)` loop that includes `COMMUNITY_MASTER_DIR . 'templates/single-tile.php'`
    - Close `</div>`

    Create `templates/single-tile.php`:
    - ABSPATH guard
    - Extract meta: `$description = get_post_meta($project->ID, '_community_master_description', true);`
    - Extract meta: `$github_url = get_post_meta($project->ID, '_community_master_github_url', true);`
    - Extract meta: `$installer = get_post_meta($project->ID, '_community_master_installer', true);`
    - Render `<div class="cm-tile">`:
      - Logo: `if (has_post_thumbnail($project->ID))` -> `<div class="cm-tile__logo">` with `get_the_post_thumbnail($project->ID, 'medium')` (FRONT-02)
      - Title: `<h3 class="cm-tile__title"><?php echo esc_html(get_the_title($project->ID)); ?></h3>` (SEC-02)
      - Description: conditional, `<p class="cm-tile__description"><?php echo esc_html($description); ?></p>` (SEC-02)
      - Installer: conditional on non-empty `$installer` (FRONT-04), `<pre><code><?php echo esc_html($installer); ?></code></pre>` (SEC-03) with `<button class="cm-copy-btn" type="button" data-copy="<?php echo esc_attr($installer); ?>">` using `esc_html__('Copy', 'community-master')` label (FRONT-05)
      - GitHub link: conditional, `<a class="cm-tile__github" href="<?php echo esc_url($github_url); ?>" target="_blank" rel="noopener noreferrer">` with `esc_html__('View on GitHub', 'community-master')` (FRONT-02, SEC-02)

    Wire into entry point -- add to `community-master.php` BEFORE the `Community_Master::instance()` call:
    ```php
    require_once COMMUNITY_MASTER_DIR . 'includes/class-shortcode.php';
    ```

    Wire into orchestrator -- add to `includes/class-community-master.php` constructor after `new CM_Admin_Columns();`:
    ```php
    new CM_Shortcode();
    ```

    IMPORTANT:
    - Every `echo` of dynamic content MUST use `esc_html()`, `esc_url()`, or `esc_attr()` (SEC-02, SEC-03)
    - Shortcode callback MUST return (not echo) via `ob_start()`/`ob_get_clean()` (pitfall from research)
    - All strings use text domain `'community-master'` with `__()` or `_e()`
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/meintechblog-community-master && php -l includes/class-shortcode.php && php -l templates/tile-grid.php && php -l templates/single-tile.php && php -l community-master.php && php -l includes/class-community-master.php && echo "ALL SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `includes/class-shortcode.php` exists and contains `add_shortcode('community-master'`
    - File `includes/class-shortcode.php` contains `wp_register_style('community-master-frontend'`
    - File `includes/class-shortcode.php` contains `wp_register_script('community-master-copy'`
    - File `includes/class-shortcode.php` contains `wp_enqueue_style('community-master-frontend')` inside render method
    - File `includes/class-shortcode.php` contains `get_posts` with `'post_type' => 'community_project'`
    - File `includes/class-shortcode.php` contains `ob_start()` and `ob_get_clean()`
    - File `includes/class-shortcode.php` contains `cm-empty-state` for empty state (FRONT-07)
    - File `templates/tile-grid.php` contains `cm-grid` class
    - File `templates/tile-grid.php` includes `single-tile.php` inside foreach
    - File `templates/single-tile.php` contains `esc_html(get_the_title(` (SEC-02)
    - File `templates/single-tile.php` contains `esc_html($description)` (SEC-02)
    - File `templates/single-tile.php` contains `esc_html($installer)` (SEC-03)
    - File `templates/single-tile.php` contains `esc_attr($installer)` on data-copy attribute (SEC-03)
    - File `templates/single-tile.php` contains `esc_url($github_url)` (SEC-02)
    - File `templates/single-tile.php` contains `target="_blank" rel="noopener noreferrer"`
    - File `templates/single-tile.php` contains `cm-copy-btn` button with `data-copy`
    - File `templates/single-tile.php` contains `has_post_thumbnail` conditional
    - File `community-master.php` contains `require_once COMMUNITY_MASTER_DIR . 'includes/class-shortcode.php'`
    - File `includes/class-community-master.php` contains `new CM_Shortcode()`
    - All 5 files pass `php -l` syntax check
  </acceptance_criteria>
  <done>
    Shortcode class registered, templates render tile grid with all fields escaped, wired into plugin bootstrap and orchestrator. PHP syntax valid on all files.
  </done>
</task>

<task type="auto">
  <name>Task 2: CSS and JavaScript assets</name>
  <files>
    assets/css/frontend.css
    assets/js/copy-installer.js
  </files>
  <read_first>
    .planning/phases/02-frontend-display/02-RESEARCH.md
    templates/single-tile.php
  </read_first>
  <action>
    Create directory structure: `assets/css/` and `assets/js/`.

    Create `assets/css/frontend.css` (FRONT-03, FRONT-06):
    - `.cm-grid`: `display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; padding: 1rem 0;`
    - `@media (max-width: 768px)` -> `.cm-grid { grid-template-columns: repeat(2, 1fr); }` (tablet: 2 columns)
    - `@media (max-width: 480px)` -> `.cm-grid { grid-template-columns: 1fr; }` (mobile: 1 column)
    - `.cm-tile`: `display: flex; flex-direction: column; border: 1px solid rgba(128, 128, 128, 0.3); border-radius: 0.5rem; padding: 1.25rem; transition: box-shadow 0.2s ease;`
    - `.cm-tile:hover`: `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);` (subtle hover, FRONT-06)
    - `.cm-tile__logo`: `text-align: center; margin-bottom: 1rem;`
    - `.cm-tile__logo img`: `max-width: 100%; height: auto; border-radius: 0.25rem;`
    - `.cm-tile__title`: `margin: 0 0 0.5rem;` (inherits theme heading styles, FRONT-06)
    - `.cm-tile__description`: `flex: 1; margin: 0 0 1rem;`
    - `.cm-tile__installer`: `position: relative; margin-bottom: 1rem;`
    - `.cm-tile__installer pre`: `margin: 0; padding: 0.75rem; overflow-x: auto; background: rgba(128, 128, 128, 0.1); border-radius: 0.25rem; font-size: 0.85em;` (FRONT-04)
    - `.cm-tile__installer code`: `white-space: pre; word-break: break-all;`
    - `.cm-copy-btn`: `position: absolute; top: 0.25rem; right: 0.25rem; padding: 0.25rem 0.5rem; font-size: 0.75em; cursor: pointer; border: 1px solid rgba(128, 128, 128, 0.3); border-radius: 0.25rem; background: inherit; color: inherit;`
    - `.cm-copy-btn--copied`: `border-color: green; color: green;` (FRONT-05 visual feedback)
    - `.cm-tile__github`: `display: inline-block; margin-top: auto; font-weight: 600;`
    - `.cm-empty-state`: `text-align: center; padding: 3rem 1rem; color: inherit; opacity: 0.6;`

    No hardcoded colors or fonts -- inherit from theme (FRONT-06). Use `inherit`, `currentColor`, and rgba for borders/shadows only.

    Create `assets/js/copy-installer.js` (FRONT-05):
    - Event delegation: `document.addEventListener('click', function(e) { ... })`
    - Find button: `var btn = e.target.closest('.cm-copy-btn');` -- return early if null
    - Get text: `var text = btn.getAttribute('data-copy');`
    - Save original label: `var originalLabel = btn.textContent;`
    - `showFeedback` function: set `btn.textContent = 'Copied!';`, add class `cm-copy-btn--copied`, setTimeout 2000ms to restore original label and remove class
    - Primary: `navigator.clipboard.writeText(text).then(showFeedback)`
    - Fallback for non-HTTPS: create hidden textarea, `document.execCommand('copy')`, remove textarea, call showFeedback
    - Wrap in `if (navigator.clipboard && navigator.clipboard.writeText) { ... } else { ... }` for fallback
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/meintechblog-community-master && test -f assets/css/frontend.css && test -f assets/js/copy-installer.js && grep -q "grid-template-columns: repeat(3" assets/css/frontend.css && grep -q "max-width: 768px" assets/css/frontend.css && grep -q "max-width: 480px" assets/css/frontend.css && grep -q "navigator.clipboard" assets/js/copy-installer.js && grep -q "cm-copy-btn--copied" assets/js/copy-installer.js && echo "ALL ASSETS OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `assets/css/frontend.css` exists
    - CSS contains `grid-template-columns: repeat(3, 1fr)` for 3-column desktop
    - CSS contains `@media (max-width: 768px)` with `repeat(2, 1fr)` for tablet
    - CSS contains `@media (max-width: 480px)` with `1fr` for mobile
    - CSS contains `.cm-tile` styles with `flex-direction: column`
    - CSS contains `.cm-copy-btn--copied` with green color for feedback
    - CSS contains `.cm-empty-state` styles
    - CSS does NOT contain hardcoded font-family or color values (uses inherit/currentColor)
    - File `assets/js/copy-installer.js` exists
    - JS contains `navigator.clipboard.writeText` for modern browsers
    - JS contains `execCommand('copy')` as fallback
    - JS contains `cm-copy-btn--copied` class toggle
    - JS contains `setTimeout` with 2000ms delay for feedback reset
    - JS uses event delegation via `document.addEventListener('click', ...)`
    - JS uses `.closest('.cm-copy-btn')` to find the button
  </acceptance_criteria>
  <done>
    CSS delivers responsive 3/2/1 column grid with theme-compatible styling. JS delivers copy-to-clipboard with visual "Copied!" feedback and fallback for non-HTTPS environments.
  </done>
</task>

</tasks>

<verification>
1. All PHP files pass `php -l` syntax validation
2. Shortcode class is wired: `community-master.php` has require_once, `class-community-master.php` has `new CM_Shortcode()`
3. Every dynamic output in templates uses `esc_html()`, `esc_url()`, or `esc_attr()`
4. CSS has 3 responsive breakpoints (desktop 3col, tablet 2col, mobile 1col)
5. JS has clipboard API with execCommand fallback and visual feedback
6. No hardcoded colors/fonts in CSS (theme-compatible)
</verification>

<success_criteria>
- `[community-master]` shortcode is registered and renders project tiles
- Grid displays 3 columns on desktop, 2 on tablet, 1 on mobile
- Each tile shows logo (if set), name, description (if set), GitHub link (if set)
- One-line installer appears only when set, with copy button
- Copy button shows "Copied!" feedback for 2 seconds
- Empty state shows "Noch keine Community-Projekte vorhanden."
- All output properly escaped (no raw dynamic content)
- CSS and JS load only on pages using the shortcode
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-display/02-01-SUMMARY.md`
</output>
